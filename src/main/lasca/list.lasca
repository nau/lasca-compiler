data List a = Nil | Cons(head: a, tail: List a)

def empty(l) = match l {
    | Nil -> true
    | Cons(_, _) -> false
}

def main() = {
    l = Cons("One", Nil);
    println("Hello world! ${l.head}. Is empty: ${empty(l)}");
}

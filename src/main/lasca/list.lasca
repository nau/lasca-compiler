data List a = Nil | Cons(head: a, tail: List a)

def empty(l) = match l {
    Nil -> true
    Cons(_, _) -> false
}

{-def foldl(l, z: b, f: a -> b -> b): b = match l {
    Nil -> z
    Cons(h, t) -> { println("Hello"); f(h, foldl(t, z, f)) }
}-}

--def length(l) = foldl(l, 0, { e, acc -> acc + 1 })

def main() = {}

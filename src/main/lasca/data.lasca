-- Records
-- Dynamic mode
data Test = Test(a: Int)

data Point = Point(x: Int, y: Int, z: Test)

def main() = {
  t = Test(3);
  p1 = Point(12, 2, t);
  x = p1.x + p1.y - p1.z.a;
  println(x.toString);
  println("Hello")
}


def foo(i: M -> F a (b c -> d)) = i

-- @ IO,
def returnArray(): Array Int = {
  array = [];
  array1 = append(array, "Hello");
  array2 = append(array1, 10.5 * 1.0);
  array3 = prepend(array2, 1);
  println(array3(2).toString);
  println(array3.toString);
--  println("2".toString); -- FIXME it's a bug
--  println(toString("2"));
--  foo(false)
}


def main() = {
  returnArray()
}


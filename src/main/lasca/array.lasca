package Array

def for(start: Int, end: Int, f: Int -> Int) = {
    if start < end then {
        f(start);
        for(start + 1, end, f);
    } else 0;
}

def returnArray(s) = {
    e = 1;
    for(0, 5, { i ->
        println(e.toString);
        for(i + 1, 5, { j ->
            s;
            println("${i} + ${j} + ${e}");
            0
        });
    });
}


def main() = {
    res = returnArray(3.14);
    println("Hello")
}


def returnArray() = {
  array = newArray();
  array1 = append(array, 2);
  array2 = append(array1, 3);
  array3 = prepend(array2, 1);
  runtimePutchar(48 + arrayApply(array3, 2));
  println(arrayToString(array3));
}

def main() = {
  returnArray()
}


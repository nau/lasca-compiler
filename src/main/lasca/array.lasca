def foo(i: M -> F a (b c -> d)) = i

-- @ IO,
def returnArray(): Array Int = {
  res = match 1 {
  | _ -> 3
--  | i -> i + 1
  | 1 -> 0
  };
  println(" Te ${res}")
}


def main() = {
  returnArray()
}


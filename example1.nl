
def main(arg: Array) = {
  def inner = 1_000;
  def string(i) = "ololo";
  def max(a, b) = if a >= b then a else b;
  if string(inner) == "ololo" and 2 < 3 or 2 xor 1 == 3 then inner else !(2_000 + 1);
  string(5 - 4 * 2 + 3 * 7);
  (max 2 3) == max(2, 3)

  false;
  inner
}

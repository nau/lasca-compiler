package Hello.Test

import Option

A = 5

--import List
def main() = {
--    list = Cons(1, Nil);
--    file = readFile("README.md");
--    println(file);
--    writeFile("test.md", file);
    s = Option.Some(1);
    str = map(s, {a -> toString(a + 1)});
    println("Hello world! ${str}")
}

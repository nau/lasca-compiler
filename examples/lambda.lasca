def test() = {
    def fact(n) = if n == 1 then 1 else n * fact(n - 1) ;
    println(toString(fact(3)))
--    println(toString(inner() + three()))
}

def main() = {
    b = println;
    test();
    c = "Hello";
    a = { s, d -> b(c) };
    a("test", 1)
}
